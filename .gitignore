# Directories
.vs/
bin/
obj/
tmp/
_bin/
_obj/

vendor/*/bin/
vendor/*/obj/
vendor/unicorn/build/

# Captured memory dumps used by spark-unicorn-test -- these come from a
# live/dumped game process and should never be committed. Keep the README
# documenting the manifest format though.
spark-unicorn-test/dumps/*
!spark-unicorn-test/dumps/README.md

# Approval-test scratch output (regenerated every run) -- the .approved.txt
# reference files next to these ARE committed.
spark-unicorn-test/approved/*.received.txt

# Files
*.vcxproj
*.vcxproj.user
*.vcxproj.filters
*.sln

scripts/user
paired_sensors.txt
cheat-engine/data

reversing/binary-ninja/*.bndb
reversing/cheat-engine/traces
reversing/ghidra/*.gpr
reversing/ghidra/*.lock
reversing/ghidra/*.lock~
reversing/ghidra/*.zip
reversing/ghidra/Halo MCC.rep/**
reversing/ghidra_restic

tags/*/bitmaps