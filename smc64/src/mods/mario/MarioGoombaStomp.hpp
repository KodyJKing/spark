#pragma once

#include "spark/mod/ModId.hpp"

/**
 * Handles logic for jumping on or ground pounding enemies heads (like Goombas).
 */
namespace Mod::Mario::GoombaStomp {
    void init(Spark::ModId modId);
}
